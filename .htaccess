Options -MultiViews
RewriteEngine On
RewriteBase /

# Serve static assets (files and directories) directly
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Everything else → let React Router handle it
RewriteRule ^ /index.html [L]
